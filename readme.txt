# README:
# To use active_record gem should be installed (if it is not please run: 'gem install active_record') and put YOUR database config inside database.rb file:
# To run script write:
# ruby run.rb
# The script is not settling anyting, nor persisting any data,
# only reading stuff from DB, and puts in standard-output, what should be the outcome of bets belonging to draw from the argument